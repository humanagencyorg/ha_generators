# frozen_string_literal: true

class <%= class_name %>Serializer
  include JSONAPI::Serializer

  set_key_transform :camel_lower

  <%- if attributes.present? -%>
  attributes <%= attributes.map(&:name).map {|x| x.dup.prepend(":")}.join(", ") %>
  <%- end -%>
end
